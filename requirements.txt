# System & Environment Requirements
Node.js >= 18.x.x
npm >= 9.x.x
Git

# Architecture Note for Judges
# -----------------------------
# SimVesto is NOT a Python Django/Flask application.
# It is entirely built on a modern MERN Stack Javascript Architecture.
# Do not attempt to use "pip install -r requirements.txt".
# 
# Please use the standard Node Package Manager (NPM) to initialize the dependencies 
# as outlined in the Execution Commands section of the README.md.

# Dependency Equivalencies (Executed via "npm install" inside both root and /backend)
vite==5.4.10
react==19.2.5
zustand==5.0.12
recharts==3.8.1
framer-motion==12.38.0
three==0.183.2
express==4.18.2
mongoose==7.0.3
groq-sdk==0.3.2 
